# Local working copy of the pristine atlases, used to keep quantise_atlases.py
# idempotent. Not committed: the atlases are procedurally generated by
# Tools/Blender/environment/build_atlases.py and cost more in LFS than they cost
# to rebuild. quantise_atlases.py refuses to run if this directory is missing and
# the atlas on disk is already its own output.
_originals/
